title: FAQs

sections:
  courses:
    # headline: Published Projects
    type: pages
    layout: list
    template: faq
    # text: "{{ page.client }}"
    # info: "{{ page.category }}"
    # status: published
    # limit: 2
    image:
      # show: false
      # ratio: 16/9
      # cover: true
