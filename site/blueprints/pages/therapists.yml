title: Therapists

sections:
  therapists:
    # headline: Published Projects
    type: pages
    layout: cards
    template: therapist
    # text: "{{ page.client }}"
    # info: "{{ page.category }}"
    # status: published
    # limit: 2
    image:
      ratio: 16/9
      cover: true
